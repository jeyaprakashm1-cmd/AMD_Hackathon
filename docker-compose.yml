version: '3.8'

services:
  app:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - .:/app
    environment:
      - ENVIRONMENT=local
      - MODEL_PROVIDER=mock
    devices:
      - "/dev/kfd:/dev/kfd"
      - "/dev/dri:/dev/dri"
